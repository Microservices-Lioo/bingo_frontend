export enum StatusRoom {
    NOT_STARTED = 'NOT_STARTED',
    STARTED = 'STARTED',
    FINISHED = 'FINISHED',
    CANCELED = 'CANCELED',
}

export enum StatusHostRoom {
    ONLINE = 'ONLINE',
    OFFLINE = 'OFFLINE',
}

export enum StatusGame {
    INICIAR = 'INICIAR',
    INICIADO = 'INICIADO',
    CONCLUIDO = 'CONCLUIDO',
    ERROR = 'ERROR',
}

export enum HostActivity {
    MEZCLANDO = 'mezclando',
    ESPERANDO = 'esperando',
    CANTANDO = 'cantando',
}